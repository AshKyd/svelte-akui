---
trigger: always_on
---

# Flexible content pattern

When we specify content in components such as icons or images, we should additionally let the implementor specify a snippet to render custom content. In item groups, we should let the implementor specify an array of items, and an optional custom snippet to render the icon/image for each item if they want to override the implementation.
